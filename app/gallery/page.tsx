//import configProvider from '../_libs/config-provider';

/* 
Fetch Photos from Your API:

Use fetch or a similar method in page.jsx to get the photos from your API route (/api/photos).
Implement a loading state and error handling.
*/
export default function Gallery() {
  return (
    <main>
      Gallery page
    </main>
  )
}
